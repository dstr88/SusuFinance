// src/components/dashboard/transactions/client/fifoGroupPolSells.ts
import { normalizePriceSymbol, parseAmount, formatUsd } from './utils';

export function fifoGroupPolSells(listEl: HTMLElement): void {
  // Your full original fifoGroupPolSells body pasted here (remove duplicates of helpers)
  // Use imported utils instead of local copies
  // ...
}