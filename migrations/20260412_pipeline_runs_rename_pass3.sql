-- No-op: tax_pipeline_runs was created by 20260412_pipeline_runs.sql with
-- the already-correct column names (pass3_fees, pass3b_defi), so there is
-- nothing to rename here. Migration kept so schema_migrations tracks it.
SELECT 1;
